# Secrets / env (never commit)
.env
.env.*
!.env.example
*.pem
*.key
id_ed25519
!id_ed25519.pub
*_private.pem
feed-signing.key
*.secret

# Machine-local operator notes (no secrets; still not shared)
# Template: docs/LOCAL_NOTES.example.md
docs/LOCAL_NOTES.md

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
!.vscode/extensions.json

# Dependencies / build (future)
node_modules/
dist/
build/
coverage/
*.tsbuildinfo
.pnpm-store/

# Local proxy runtime (future) — do not ignore dashboard/data (static demo JSON)
*.sqlite
*.sqlite-journal
/data/
packages/proxy/data/
logs/
.cache/
