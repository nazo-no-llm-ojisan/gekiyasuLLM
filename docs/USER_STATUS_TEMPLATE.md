# ユーザ向け現状報告テンプレ（脳を使わない版）

報告するとき・自分用メモのとき、**このままコピペして穴埋め**する。  
技術詳細は書かない。長くしない。

---

## いま（超短）

**できてる:** （例: 設計メモ＋PC用の中継箱 `127.0.0.1:16191`）  
**できてない:** （例: 安いAIを自動で選ぶところ）

中継＝電話の延長コード。秘書ではない。

---

## 今日やること（1つだけ）

1. （例: `packages/proxy` で `npm run dev`）
2. （例: キーを環境変数）
3. （例: 接続先 `http://127.0.0.1:16191/v1`）
4. （例: 一言チャット）

→ **通ったら勝ち。** 通らなきゃキー／起動／URLだけ疑う。

---

## 順番（迷ったら上から・飛ばさない）

1. 中継が通る
2. 「どれに送るか」の計画を出す
3. 失敗したら次の候補
4. 値段の一覧表（手書きJSONでOK）
5. そのあと自動化

**触らない:** スクレイピング・Webサイト・広告（今は）

---

## 口癖

- 迷ったら「中継が通るか」に戻る
- 一度に一つのことだけ
- 「賢く」しない。動いたらコミット

---

## エージェントへの返し方（最短）

次のメッセージはどれか1つでよい:

- `起動コマンドだけ`
- `通った`
- `通らない`（できれば: キー入れた？ / 起動した？ / URLは？）
- `次の一手だけ`

エージェントは **次の一手だけ** 返す。長文の講義をしない。

---

## 記入例（2026-07-12 時点のスナップショット）

**できてる:** 設計メモ＋PC用の中継箱（`127.0.0.1:16191`）  
**できてない:** 安いAIを自動で選ぶところ  

**今日:** 中継1本通す  

**順番の位置:** 1（中継が通る）
