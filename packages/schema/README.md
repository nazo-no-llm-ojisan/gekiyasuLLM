# @gekiyasu/schema

Shared **domain types** for gekiyasuLLM: Evidence, Offering, RoutePlan, pricing normalization, probes, upstream internal shapes.

No runtime logic — types only (plus port constants).

```bash
npm install
npm run typecheck
npm run build
```

See `docs/design/05-adapters-normalization-routing.md`.
